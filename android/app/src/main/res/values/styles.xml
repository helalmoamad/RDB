<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off.
         Uses the Android 12+ Splash Screen API (backported via androidx.core:core-splashscreen).
         postSplashScreenTheme hands the window off to NormalTheme once the splash dismisses,
         which lets the Flutter UI render smoothly and avoids the splash hang on Android 12/13. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <!-- خلفية بيضاء بالكامل -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- الأيقونة بنفس لون الخلفية (أبيض على أبيض) لتختفي تمامًا.
             Android 12+ يفرض عرض أيقونة، فنجعلها غير مرئية بدل حذفها. -->
        <item name="windowSplashScreenAnimatedIcon">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
